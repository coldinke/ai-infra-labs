# Day 02

Initialized the softmax lab with a PyTorch reference variant, a manual eager
stable softmax baseline, correctness tests, and a shared benchmark/profile
entrypoint. Added the CUDA extension scaffold and a naive row-wise fp32 kernel
variant as the baseline for later optimization.
